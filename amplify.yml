version: 1
backend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm start
  artifacts:
    files:
      - '**/*'
  cache:
    paths:
      - node_modules/**/*
