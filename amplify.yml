version: 1
applications:
  - backend:
      phases:
        preBuild:
          commands:
            - npm ci
        build:
          commands:
            - npm start
      artifacts:
        files:
          - '**/*'
      cache:
        paths:
          - node_modules/**/*
